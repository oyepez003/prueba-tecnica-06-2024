services:
  app:
    container_name: app
    image: app-dev
    build:
      context: .
    restart: "no"
    volumes:
      - .:/app
    ports:
      - 8000:8000
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
    db-data:
        driver: local
